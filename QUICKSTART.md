# webscoop 快速上手（给新朋友）

这是一个「网站资源下载工具」：把网页/网站里的 **图片、视频、音频、文档、软件安装包** 一键抓到本地。
不需要写任何代码，全程鼠标点一点就行。

---

## 一分钟上手

1. **双击 `start.bat`**
   - 第一次运行会自动建虚拟环境、装依赖（可能需要几分钟），装完自动弹出窗口。
   - 系统提示缺少 Python 的话，先装 [Python 3.10+](https://www.python.org/downloads/) 并勾选
     “Add Python to PATH”，再双击 start.bat。

2. **输入网址 → 点「发现资源」**
   - 左侧列表马上会一条条列出抓到的资源（流式显示，不用等全部抓完）。
   - 图片显示缩略图，视频显示封面，文件显示类型和大小。

3. **勾选想要的 → 点「下载勾选」**
   - 点一行 = 勾选；`Shift+点击` 范围多选；`Ctrl+点击` 单独反转。
   - 下载的文件自动按类型存到项目 `information/` 目录：
     `images/`（图） `videos/`（视频） `audios/`（音频） `docs/`（文档） `software/`（安装包）等。
   - 已存在的同名文件会自动跳过，不会重复下载、不会覆盖。

完成。就是这么简单。

---

## 界面按钮速查

| 按钮 | 作用 |
|---|---|
| 发现资源 | 扫描当前网址里的资源（自动翻页、跟详情页） |
| 停止 | 提前结束发现，已显示的保留 |
| 下载勾选 | 下载所有勾选的资源 |
| 备用下载(gallery-dl) | 内置发现器认不出的站交给它（支持 1400+ 网站兜底） |
| API 抓取… | 用 Pexels 官方接口抓图/视频（可选，需自己的 Key） |
| LLM 模型… | 用 AI 生成「高清大图规则」，高级功能，可选 |
| 打开页面 | 在浏览器打开勾选资源对应的网页 |
| 全选 / 反选 / 全不选 | 批量勾选 |
| 右键列表项 | 预览、查看高清原图、按类型全选、复制链接等 |

缩略图加载失败显示灰底占位图，属正常，不影响下载。

---

## 常见使用场景

### 1. 普通图片/素材站（壁纸、设计素材、图库）
输入网址 → 发现资源 → 勾选 → 下载。通常直接能用。

### 2. 网页要滚动/点了才加载内容的（JS 动态加载）
在「抓取设置」里勾上 **渲染模式**（用无头浏览器真实渲染页面后再抓），再点发现资源。
（首次用渲染模式需装一次浏览器内核：`python -m playwright install chromium`）

### 3. 内置发现器抓不到的特殊网站（Instagram、Pinterest、Reddit、图库投稿站等）
点 **「备用下载(gallery-dl)」**，设置数量/类型后它会列出候选文件，预览勾选后下载。

### 4. 找到的资源只有缩略图，想要高清原图
右击资源 → **查看高清原图**。内置规则会自动解析高清直链；
个别新网站规则不认识时，可用「LLM 模型…」让 AI 分析一页自动生成规则（一次配好永久生效）。

### 5. 想在播视频/听音频
右击视频资源 → **放大/播放**（内置 VLC 播放器）；大视频先缓存后本地播放，不卡顿。

---

## 能抓什么 / 不能抓什么（如实说明）

**能抓**：链接直接暴露在网页或接口里的图片/视频/音频/文档/安装包。
项目自带多种反爬应对：随机浏览器指纹（TLS/JA3）、代理、随机延迟、自动重试。

**抓不到（请理解，不是 bug）**：
- 需要登录账号 / 付费会员才看得到的资源
- 必须点击、拖拽、发消息才能触发加载的内容
- 只能通过专门的 App 查看的内容
- 资源网址做了防下载签名加密的

---

## 常见问题

**Q：下载很慢或访问国外网站失败？**
A：本程序默认**直连**（无需任何配置，开箱即用）。如果你访问国外网站需要代理，把你自己
的代理地址填到 `config.py` 第 20 行左右：

```python
DEFAULT_PROXY = "http://127.0.0.1:7890"   # 换成你自己的代理地址（如 Clash/v2rayN 的端口）
PROXY_ENABLED = True                      # 填好地址后把这里改成 True
```

不确定自己的代理端口？打开你用的加速工具（Clash/v2rayN 等），看它的「本地端口/混合端口」，
一般是 `7890`、`1080`、`10808` 之类。改好后重启程序，或在主界面勾上「启用代理」。

**Q：双击 start.bat 黑窗口一闪而过？**
A：一般是依赖安装失败。在项目目录打开命令行手动装：
```bash
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt
.venv\Scripts\python gui.py
```

**Q：预览里视频放不了？**
A：播放组件首次运行会自动准备（需要能下载 VLC 的联网环境）。直接点「放大/播放」，
如果提示播放器不可用，按提示安装 `pip install python-vlc` 或安装 VLC 桌面版。

**Q：怎么确认程序正常工作？**
A：一句话跑全部自测（不用联网）：
```bash
.venv\Scripts\python tests\run_all.py
```
全部显示 `PASS` 即为正常。

---

## 给新手的三条提醒

1. **只抓你被允许抓的内容**，尊重网站的 robots.txt 和服务条款，不用于侵权。
2. 下载的素材注意授权协议（免费图库一般允许使用，但商用/署名规则各不同）。
3. 本项目仅供学习与合法用途。抓取行为请遵循当地法律法规。
